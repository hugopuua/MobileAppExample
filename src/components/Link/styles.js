import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        paddingHorizontal: 8,
        paddingVertical:20,
        borderRadius:8,
        marginTop:30
    },
    title: {
        color: '#4F63AC',
        fontSize: 16,
        fontWeight: 'bold',
        textAlign:'center'
    }
})